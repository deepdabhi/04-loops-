#include <stdio.h>
int main(){
    int n;
    printf("Enter n : \n");
    scanf("%d",&n);
    for(int row = 1; row <= n; row++)
    {
        int count = 1;
        while(count <= row)
        {
            if(row % 2 == 0)
            {
                
            }
        }
    }
return 0;
}